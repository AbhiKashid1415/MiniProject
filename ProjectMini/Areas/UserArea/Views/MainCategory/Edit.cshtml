@model ProjectMini.Models.MainCategory
@{
    ViewBag.Title = "Edit";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="text-center mb-4">Edit MainCategory Details</h2>
            @using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "p-3" }))
            {
                @Html.HiddenFor(p => p.MainCategoryId)

                <div class="form-group mb-3">
                    @Html.LabelFor(p => p.MainCategoryName, new { @class = "form-label" })
                    @Html.EditorFor(p => p.MainCategoryName, new { htmlAttributes = new { @class = "form-control", placeholder = "Enter category name" } })
                </div>

                <div class="text-center">
                    <input type="submit" value="Update" class="btn btn-primary px-4" />
                </div>
                <div class="text-center" style="font-size:18px">
                    <hr />
                    @Html.ActionLink("Back to list", "Index", null, new { @class = "text-decoration-none" })
                    <hr />
                </div>
            }
        </div>
    </div>
</div>
